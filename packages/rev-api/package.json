{
  "name": "rev-api",
  "version": "0.0.1",
  "description": "RevJS Isomorphic Data Library - API Library",
  "files": [
    "lib/"
  ],
  "scripts": {
    "build": "run-bin tsc",
    "build-watch": "run-bin tsc --watch",
    "lint": "run-bin tslint ./src/**/*.ts",
    "test": "run-bin mocha -r ts-node/register --recursive \"./src/**/__tests__/*\"",
    "test-watch": "run-bin mocha -r ts-node/register --recursive \"./src/**/__tests__/*\" --watch",
    "mocha": "run-bin mocha -r ts-node/register"
  },
  "author": "Russell Briggs <russ@paraflyer.net>",
  "license": "MIT",
  "typings": "lib/index.d.ts",
  "dependencies": {
    "rev-models": "0.0.1"
  },
  "devDependencies": {
    "run-bin": "1.0.0"
  }
}
