{
  "name": "rev-backend-mongodb",
  "version": "0.9.1",
  "description": "RevJS Isomorphic Data Library - Mongo DB Backend",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "run-bin tslint -p . && run-bin tsc",
    "build-watch": "run-bin tsc --watch",
    "test": "echo SKIPPING run-bin mocha -r ts-node/register --recursive \"./src/**/__tests__/*\"",
    "test-ci": "echo SKIPPING run-bin mocha -r ts-node/register --recursive \"./src/**/__tests__/*\" --reporter mocha-circleci-reporter",
    "test-watch": "echo SKIPPING run-bin mocha -r ts-node/register --recursive \"./src/**/__tests__/*\" --watch --watch-extensions ts,tsx",
    "mocha": "run-bin mocha -r ts-node/register",
    "pack": "npm pack"
  },
  "author": "Russell Briggs <russ@paraflyer.net>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/RevJS/revjs.git"
  },
  "typings": "lib/index.d.ts",
  "dependencies": {
    "mongodb": "^3.0.2",
    "rev-models": "^0.9.1"
  },
  "devDependencies": {
    "@types/mongodb": "^3.0.5",
    "run-bin": "^1.0.0"
  }
}
